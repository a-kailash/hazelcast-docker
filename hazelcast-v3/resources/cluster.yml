hazelcast:
  group:
    name: dev
    password: dev-passhazelcast:
  network:
    port:
      auto-increment: true
      port-count: 10
      port: 5701
    join:
      multicast:
        enabled: false
      tcp-ip:
        enabled: true
        member-list:
          - hc-member1 
          - hc-member2 
          - hc-member3 
